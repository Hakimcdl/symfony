<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
    <h1>Le {{ prod.Name }} Produits incroyables</h1>
    <p>Voici la description du produit{{ prod.Description }}</p>
    <span>Le stock restant est de : {{ prod.Stock }}</span>
    <a href="{{ path('changeActiveProd', {'id':prod.id}) }}">
        {% if(prod.active == true) %}
            Désactiver
        {% else %}
             Activer
        {% endif %}
    </a>
    {% if(prod.active == false) %}
         <a  href="{{ path('deleteUnactiveProd', {'id':prod.id}) }}">Supprimer</a>
    {% endif %}
</body>
</html>